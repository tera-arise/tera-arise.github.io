{
  "$schema": "https://json.schemastore.org/package.json",
  "private": true,
  "engines": {
    "node": ">=18.0.0"
  },
  "devDependencies": {
    "@11ty/eleventy": "2.0.1",
    "@11ty/eleventy-plugin-directory-output": "1.0.1",
    "@11ty/eleventy-plugin-syntaxhighlight": "5.0.0",
    "markdownlint-cli2": "0.13.0"
  },
  "scripts": {
    "build": "npx markdownlint-cli2 && npx @11ty/eleventy",
    "serve": "npx @11ty/eleventy --serve --port 54321"
  }
}
